# Event Delegation

- Capturing and bubbling allow us to implement one of the most powerful event handling patterns called event delegation.

- The idea is to when we have lot of elements to handled in a similar way , then instead of assigning handler to each of them - we put single handler on their common ancestors.

## Delegation Example

- Capturing and bubbling allow us to implement one of the most powerful event handling patterns called event delegation.

-
